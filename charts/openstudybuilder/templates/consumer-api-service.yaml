{{- if .Values.consumerApi.service.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.consumerApi.fullnameOverride }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: "consumer-api"
spec:
  type: {{ .Values.consumerApi.service.type }}
  ports:
    - port: {{ .Values.consumerApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: "consumer-api"
{{- end }}
